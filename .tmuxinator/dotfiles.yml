# ~/.tmuxinator/dotfiles.yml

name: dotfiles
root: ~/
startup_window: dotfiles
startup_pane: 1

windows:
  - console: dotfiles status
  - dotfiles:
      panes:
        - vim -O ~/.vimrc ~/.tmux.conf ~/.zshrc
