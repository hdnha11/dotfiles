source ~/.profile

# Load secrets from chezmoi keyring if .secrets config exists
if [ -f ~/.secrets ] && [ -x ~/.local/bin/load-secrets ]; then
    source <(~/.local/bin/load-secrets ~/.secrets 2>/dev/null)
fi
